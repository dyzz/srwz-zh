# Special Disc 编辑参考

`roster.json` 和 `sd-terms.json` 保存 2026-09-18 初稿使用的角色卡与 SD 术语预处理结果，从本地翻译工作包原样迁入。提案和暂定字段保持原样；它们尚不是统一获批的共享词表。

实际译稿仍在 `corpus/zh/special-disc/`。模型批次、返回结果和审阅表保留在 `work/authoring/special-disc/translation/`。

`stage-native-overrides.json` 登记首批两关的 19 处按位置复用决策，包含原文哈希与选择理由，状态保持 draft。它不覆盖五份 SP 原始译稿。
