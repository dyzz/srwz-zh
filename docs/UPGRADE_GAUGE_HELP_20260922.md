# 改造量表帮助文案修正

用户提供的改造界面截图显示，“已经改造的阶段以＜小改造量表亮＞显示。”将占位名称直接显示给玩家。截图确认了可见文案问题，但不能单凭截图确定原生程序是否曾计划将该名称替换为图形，也未据此断言截图来自哪个版本。

本篇正式语料存在相同的六条文案；BEST 由同一份本篇语料编译生成。已同步修改本篇 `corpus/zh/menu/remaining-ui.json` 和 SP `corpus/zh/special-disc/instructions-overrides.json` 的六条帮助；SP 使用固定槽覆盖，避免继续从已冻结的本篇旧构建继承旧句。

统一第二行为：**亮起的格数表示已完成的改造阶段。**

第一行分别为：

- 量表显示当前最大HP及改造进度。
- 量表显示当前最大EN及改造进度。
- 量表显示当前装甲值及改造进度。
- 量表显示当前运动性及改造进度。
- 量表显示当前瞄准值及改造进度。
- 量表显示武器当前的改造RANK。

保留 HP、EN、RANK 等界面标识，不对原生日文或收到的审阅文件作修改。没有新增字形需求。

本篇槽位：0x7B570、0x7B5F0、0x7B670、0x7B6E0、0x7B760、0x7B7E0。
SP 槽位：0x91AA0、0x91B20、0x91BA0、0x91C10、0x91C90、0x91D10。

两版原生日文逐条一致。使用本篇实际固定槽写入函数和 SP 覆盖写入函数验证：编码回读一致、固定槽容量充足、SP 指针保持不变、全部非目标范围字节不变。每条两行，最长 16 字符；写入 64—66 字节，至少剩余 38 字节。完整证据见 `work/verification/upgrade-gauge-help-20260922/proof.json`，用户原截图保存为同目录 `user-before.png`。

已通过 BEST 实际后端 `BestCompiler.compile_compdata` 验证：六条新文案均正确进入 BEST 输出，非目标输出字节、BEST 原生代码基址和四项原生数据修正保持不变。证据见同目录 `best-proof.json`。

当前完成本篇、BEST、SP 的语料落地与组件级校验；正式 ISO 不在本次发布范围，尚无修正后运行截图。审阅站未更新。
