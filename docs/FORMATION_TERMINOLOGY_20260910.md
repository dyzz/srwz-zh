# 阵型术语统一与英文标题反馈

2026-09-10。根据用户提供的六张游戏截图及文字建议，中文显示统一使用
**TRI阵型／集中阵型／分散阵型**，简称为 **TRI／集中／分散**。
“队形”统一改为“阵型”。Center 按集中攻击一个目标的特点译为“集中”。
经用户再次确认，Wide 最终定为“分散”，对应队长与队员散开、分别攻击目标；
“广域屏障”“屏障广域化”等独立能力名称保留。

## 修改范围

共修改 30 个中文语料文件中的 246 个显示字段：剧情对话 99、菜单与帮助 134、
战斗字幕 8、图鉴正文 2、战斗贴图标签 3；另同步术语表并新增 Center 条目。
原文、稳定 ID、原文哈希、记录结构和已有控制信息保持不变。

- stg_501 对应 `stage-186.json`；图 1 的两处相同对话为
  `story/186/dialogue/01.24/0021` 与 `story/186/dialogue/01.26/0020`，现在均为
  `“TRI阵型、集中阵型、\n　分散阵型……”`。
- 同步主线教学及 stg_500，移除 `CENTER`、`Wide`、`宽幅`、`三重`作为阵型名称的混用。
  stg_500 对“TRI阵型”的索引提示也不再错误引用“三”。
- 图 2 的 NISVDATA 教学第 4–7 页、Q&A、屏障能力说明、阵型选择、批量设置、
  菜单帮助、地图简称及预设小队名称均同步；T／C／W 原有标记保留。
- 菜单固定地址包括 COMPDATA `0x7F580/0x7F5A0/0x7F5C0` 的全名，
  `0x7F5E0/0x7F5E8/0x7F5F8` 的简称，以及 SLPS `0x345DC8/0x345DD0/0x345DE0`。
- 对其他实际显示“队形”的台词和招式名称一并更正，并同步相关图鉴与武器术语，
  包括“阵型·加贡多拉”。历史发布记录及旧验收文档保留原貌。

## 战斗贴图

`BTL/TRICMN.BIN` 的三个阵型标题使用原有字体、材质、位置和索引绘制方式重新生成。
相对 2026-09-02 的冻结贴图，只改变这三个标题矩形内的 9,181 个逻辑索引；其他逻辑像素、
调色板、透明度定义、TIM2 头、SEG、成员大小及非图像字节均保持一致。

新组件为 677,424 字节，SHA-256：
`10d5ad0d907e7e1038c35206b6a586cf093f96b017011be8a055a5a7021347f8`。

相对前一轮“TRI／集中／广域”的候选，仅“分散阵型”矩形内的 2,834 个逻辑索引改变，
TRI、集中及其余贴图内容逐像素一致。

冻结快照现在绑定中文语料哈希，防止语料改了却继续悄悄注入旧贴图。
显式重新冻结只产生静态验证记录，不能复制旧镜像的人工运行验收。
2026-09-02 的历史验收保存在 `previous_runtime_acceptance` 下，新快照为 `runtime=pending`。
组件组合器可以组合静态验证通过的候选，同时保留待运行复验状态。

## 图 3–6 的英文标题

术语提交 `dd81244` 当时保留以下英文，登记为后续贴图汉化项。
用户随后确认“这几个也处理”，这六项现已完成源码与组件修改；贴图来源、构建接入和
运行验证边界见 [阵型与说明标题汉化记录](UI_FORMATION_HELP_HEADINGS_20260910.md)。

| 截图与入口 | 原英文 | 后续采用译文 |
| --- | --- | --- |
| 图 3 敌方小队预览；图 4、5 阵型预览 | FORMATION | 阵型 |
| 图 4 单队阵型；图 5 全体指令 → 批量设置阵型 | SELECT-FORMATION | 选择阵型 |
| 图 4 右侧队员排列 | SELECT-SORT | 选择排列 |
| 图 6，按 SELECT 弹出说明 | Key Help | 按键说明 |
| 图 6 | OTHERS COMMAND | 其他指令 |
| 图 6 | DATA HELP | 数据说明 |

图中英文旁边的中文阵型名称属于本轮已修改范围。
用户原图及 SHA-256 在本地
`work/review/formation-terminology-20260910/screenshot-evidence.json` 留存。
截图未提供可核对的镜像哈希，作为问题现象证据使用。

## 验证与交付边界

- 前一轮完整 270 项单元测试通过；最终改为“分散”后，27 项贴图、Q&A、阵型布局及术语测试通过。
  `compileall` 与 `git diff --check` 通过。
- 剧情组件成功重建：170 个含译文 STAGE、84,338 条记录，固定容量满足。
- 教学 10 页／114 条记录回读一致，记录样式、原有坐标契约、分页容量及其他块保持。
- Q&A 102 页／2,609 条正文记录及 264 条元数据回读一致；固定列对齐、样式及分配表保持。
  枚举项原有尾随间隔保留，经 NISV 编码器写入两字节空格。
- 24 处变动的 COMPDATA／SLPS 固定文本槽分别编码写回并回读，最小余量 4 字节，
  无须扩展槽位，指针和控制标记保持。
- 4 个预设阵型名称在锁定清单中的 8 个实际槽位全部可容纳，最小余量 12 字节。
- 中文语料审计确认：旧“队形”及旧阵型名无残留，原文、ID、哈希与记录布局未变。
- TRICMN 静态预览已检查，正常构建从新冻结快照回读一致，并明确报告 `runtime=pending`。

最终修订记录位于本地 `work/review/formation-terms-final-20260910/`，包含
[三种阵型贴图预览](../work/review/formation-terms-final-20260910/formation-labels.png)。
上一轮记录保留在 `work/review/formation-terminology-20260910/`。
这些是源码、组件和静态回读证据。本轮没有重建整盘 ISO、制作发行补丁或进行 PCSX2／
LRPS2 游戏内复验。本次提交包含相关语料、贴图快照、构建与验证代码及本记录。
后续整盘构建需要正常刷新中文输入与组件锁，
再在精确新镜像上复验 stg_501、单队／批量阵型、Q&A 以及战斗动画标题。
