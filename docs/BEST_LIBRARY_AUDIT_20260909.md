# BEST 资料库其他功能检查

2026-09-09。检查剧情流程修复后的 `current-best.iso`：
`33e71eabc2aa2c84799e2d24c57aa78f0035e8a4633a1964735bfbb3e392894f`。

结论：从标题画面进入资料库，其他五个入口未发现黑屏、卡死、加载失败或无法返回。
本轮未改游戏程序、语料或 ISO。发现一处低优先级 Q&A 标点换行，单独记录如下。
结论属于当前精确镜像的静态检查和 LRPS2 运行检查。按 2026-09-09 用户确认的
验收标准，LRPS2 通过即视为通过，无需另做 PCSX2 验证；下述覆盖范围保持不变。

## 功能覆盖

| 入口 | 静态覆盖 | LRPS2 实测 |
| --- | --- | --- |
| 机体图鉴 | 321 条全部解压、解析；图像字段与 Best 原盘一致 | 14 页全部遍历；名称／作品排序切换、正文滚动、R1 切换机体；魔神 Z、戴安娜 A、原型、末页雷欧详情及返回正常 |
| 角色事典 | 411 条全部解压、解析；头像和 VOIC 字段与 Best 原盘一致 | 12 页全部遍历；排序、正文滚动、R1 切换角色、圆圈表情、方块台词按钮；兜甲儿、弓沙也加、安雅、末页布拉亚详情及返回正常 |
| 术语事典 | 52 条全部解压、解析 | 3 页全部遍历；排序、L1／R1 前后切换弹窗；宇宙科学研究所、恐龙帝国、阿恶、末页补陀落详情及返回正常 |
| 音乐选择 | 101 个曲名所在数据区与 Best 原盘逐字节一致 | 11 页全部遍历；选择、播放、前后换页和返回正常；包括第 11 页唯一曲目 `BANPRESTO! Ver.Z` |
| 攻略 Q&A | Best 第 6 块与同批中文前端逐字节一致，绑定其 102 页、2609 条文本记录回读 | 战斗／编成／强化／其他四类均进入主题、问题及答案正文；长文滚动、逐层返回资料库正常 |

图鉴共 784 条记录全量静态检查，不代表已在模拟器里逐条打开 784 个详情。
运行遍历了全部列表页，并抽查上表详情。Q&A 正文抽查“什么是队形？”“什么是小队？”
“什么是驾驶员养成？”“什么是 SR 点数？”，没有逐页运行全部 102 个答案。

角色详情额外执行了“圆圈／方块按键”和“不按键”的同帧对照。3360 帧的头像完全
一致，圆圈输入后出现不同表情；3510 帧对照仍为原表情，按键组已显示笑脸，头像
区域有 4717 个像素不同。方块输入后的音频 RMS 为不按键对照的约 1.98 倍。
这补齐了代表角色的按钮响应证据，不代表穷尽全部角色的所有表情和台词。
探索时在列表发送 L1 没有翻页；上述全页覆盖来自
随后按方向键右的独立遍历，不能把早期文件名中的 `page-boundary` 当作已经换页。

## 音频与加载表

固定 LRPS2 软件渲染器下记录了 48 kHz 双声道 PCM。兜甲儿台词按钮按下后的窗口、
音乐前三个抽样曲目和末页曲目均有非零音频输出。音乐抽样为 `マジンガーZ`、
`おれはグレートマジンガー`、`あしたに生きるバルディオス`、`BANPRESTO! Ver.Z`。
这证明选定操作后的音频回调有输出，不代替逐句／逐首听音或完整音质验收。

实际 ISO 的三个图鉴、NISVDATA、COMPDATA 和 ELF 均与已验证组件哈希一致。
再次搜索原生 ELF 中 13 组完整偏移表：只有 HSFC 存在第二份完整匹配，其消费者
已经由前一轮修复同步；本轮未找到其他同型遗漏。该搜索结果不等同于证明程序内
不存在任何形式的间接或局部偏移引用，功能结论同时依赖上述实际运行路线。

## 低优先级排版记录

`V040-COMMON-QA-093-PUNCTUATION`（原检查编号 `BEST-LIBRARY-QA-093-PUNCTUATION`）：
其他 → 关于 SR 点数 → 什么是 SR 点数？
正文中的彩色“SR 点数获得不可”后，句号单独换到下一行。

GitHub：[issue #22](https://github.com/dyzz/srwz-zh/issues/22)（两版共通，未修改）。

后续已定位为跨颜色记录的整条换行缺少行首标点约束，详见
[Q&A 格式排查](QA_PUNCTUATION_LAYOUT_AUDIT_20260909.md)。全量扫描有 47 处行首标点
候选，其中 6 处整行只有句号；候选范围不等同于逐页运行确认。

- 关联反馈人：**AL-E**；在其剧情流程黑屏反馈后的资料库扩展检查中记录。
- 影响 **Original 和 The Best 两版**。从 v0.4.0 两版冻结发行 ISO 和两版当前 ISO
  直接读回并解压 `DATA/NISVDATA.BIN` 第 6 块，四份字节完全一致，SHA-256 均为
  `6a9ea5433fb25ebfad56ea6ab1586bab603ec1dbd4d552cd3240304759355cbe`。
  两版共用该处文本／样式记录；运行截图来自 Best，Original 未单独截图复验。
- 语料：`corpus/zh/menu/nisv-strategy-qa.json`，`page/093/record/014`。
- 当前译文是独立样式记录 `。`，运行画面证据为
  `work/runtime/lrps2/best-library-audit-20260909/qa-answers/frames/08161-qa-other-answer.png`。
- 答案仍能滚动和返回；本轮登记为排版问题，未修改文本或布局。

## 证据

机器可读汇总：[`library-audit-20260909.json`](../manifests/editions/best/library-audit-20260909.json)。
17 次成功运行、214 张截图，源记忆卡副本的哈希均保持不变；关键详情、返回画面和
完整列表页码已查看。运行根目录为 `work/runtime/lrps2/best-library-audit-20260909/`，
场景、静态审计脚本、音频采集脚本及日志位于 `work/analysis/best-library-audit-20260909/`。

最初两个音频探针因本地 Python 的 `array.array` 无 `clear()` 方法，在第一帧后失败；
改为切片清空后，以 `*-deep-audio-v2` 重跑通过。失败发生在诊断前端且没有目标界面
截图，不能作为游戏故障。所有原始记录保留，汇总明确区分这些失败探针和成功运行。
