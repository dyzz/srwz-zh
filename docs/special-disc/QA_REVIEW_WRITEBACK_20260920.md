# SP Q&A 中文校订与排版写回（2026-09-20）

用户提供的 `qa-japanese-20260920-中文校订.zip` 已核对、导入，并写入当前 SP ISO。适用范围是 Q&A；这不是整个 SP 的日文清零报告。

## 校订来源

- ZIP SHA-256：`2c48cd69b6e7fbf599b877ba3df34cde41798edf35089ec66b176a8ea0c329ee`。
- 37 个条目 ID、顺序及日文原文哈希全部匹配原导出；64 个受保护文件逐字节一致，包含原始二进制、坐标、颜色和原文。
- 仅从 `qa-review.json` 读取校订译文和备注；包内其他文档不作为操作指令。
- 导入 `corpus/zh/special-disc/native-text.json`，保留先前译文和审校来源历史。
- 22 页正文：15、32、35、59、60、70、71、75、79、81、82、88、89、93、94、95、96、97、99、100、101、102。
- 15 条元数据：3 个问题标题、12 条关键词说明。正文保留全部校订文字；换行、段落、颜色及表格位置由独立绑定配置确定。

导入证据：`manifests/sp-qa-review-import-20260920.json`。

## 排版与特殊格式

`config/products/special-disc/qa-layout.json` 显式绑定标题、段落、表格与语义颜色。正文使用原有 2:0、2:14、2:3、2:7 样式；标题沿用 4:0、4:6。不把整页译文写成单色长字符串。

保留按钮符号 △、○、□，以及 START、SELECT、L1/R1、CONTINUE、NORMAL、数字和百分比。两字节正文流的可见空格统一编码为 `0x8140`；关键词说明中的全角空格及实际换行保留。不对用户译文做 Unicode 兼容归一化。

数值与缩写跨颜色分段时仍作为连续文本判断合法断点，保护 `0.5%`、`1.2倍`、`10 EN`、`MAP`、`SR点数` 等。第 15 页的加成对照、第 60 页的能力／机体／概率表格分别保留列结构。

### 实际窗口裁切修正

首轮静态检查使用了既有 `x=532` 上限；LRPS2 显示这一字格会被 Q&A 窗口裁掉。例如第 102 页的句号和“具体内容”的“具”有二进制记录，却没有显示。最终版将最右完整字格收紧到 `x=513`，并重新排版全部页面。

共通页面共有 48 页发生排版修正：50 处右侧裁切风险、32 处其余断行修正，共 82 处。部分原先发现的标点／数字断行已在收紧边界时同时消除，不能把两个轮次的计数相加。所有共通页面的有序文字及颜色序列与当前 Original 编译产物一致。

## 静态与 ISO 验证

- 102 页正文、264 条元数据全部回读。
- 22 页新译正文和 15 条元数据与用户校订文字完全一致。
- 日文假名残留 0；SP 与原版有差异的 37 个字段全部写回，不再保留日文原文。
- 行首闭合标点、行尾左括号、越界、重叠、实际数字／缩写断行均为 0。
- 数字扫描另有 4 个已核对的非问题：第 36 页的体型 S／5 个等级，第 37、43 页的等级表／点数表头，第 83 页不同段落的 2／MAP。
- 分配表、元数据索引、102 页分配范围及图片段保持原样。记录数量由位置重排产生变化，按文本段长度正常解析，共 2,222 条。
- 压缩数据 34,580 / 39,600 字节；最紧的页面仍留有 49 字节填充。没有扩大页分配或移动后续 LBA。
- 写回严格限定于 Q&A 压缩槽；其余 3,791,742,288 字节的 ISO 范围一致。
- `python3 -m unittest discover -s tests -p '*qa*.py'`：14 项通过，包含幂等性、颜色序列、数字保护、边框裁切及固定列验证。

独立校验入口：

```sh
python3 tools/special_disc/verification/verify_qa_writeback.py --output manifests/sp-qa-writeback-20260920.json
python3 tools/special_disc/verification/audit_qa.py --output manifests/sp-qa-reviewed-audit-20260920.json
```

## 当前产物

- ISO：`build/iso/special-disc/sp-current.iso`
- SHA-256：`edeb5e81dc14cea803d5fea2cd219d86134d466b43b51998308e54bddfa46280`
- ISO 大小：3,791,781,888 字节。
- 写回器：`tools/special_disc/writeback/qa_native.py`。
- 原子更新入口：`tools/special_disc/writeback/build_qa_layout_candidate.py`。
- 已接入 `build_full_text.py`，后续完整 SP 构建继续应用此次语料和布局。
- 全页坐标示意：`work/authoring/special-disc/qa-20260920/review.html`；系统字体示意不替代运行截图。

## 运行证据

最终 ISO 使用 LRPS2 Software renderer，已逐页打开全部 102 页，并对 70 页执行滚动至末尾检查，共保留 172 张截图。问题标题逐页 OCR 对照确认导航覆盖；5 个轻微 OCR 差异另行记录。已目视复核全部 22 页新译正文的首屏，并抽查共通页面及滚动末尾；这不等同于每一行都做过人工图像检查。第 12 页的 `0.5%`／`1.2倍`、第 60 页表格及第 102 页此前缺失的句号和“具”均有最终运行截图。

运行过程中只发送按键，不修改模拟器内存；源记忆卡哈希保持不变。证据绑定最终 ISO 哈希，不混用早期候选截图。最终目录为 `work/runtime/lrps2/sp-qa-review-20260920/final-513/`，运行摘要为 `manifests/sp-qa-runtime-20260920.json`。

早期目录分页导航存在行号保留行为，初次采集编号不可信；那些文件不作为最终逐页覆盖证明。最终流程修正为第二页目录先保持行号翻页，再移动至第一个主题，并按问题标题核对。

## Issue #24

本次完成的是 SP Q&A。SP 原先继承的行首标点问题已经修正，SP 专有日文页面也已写回。Original / The Best 的另外 44 处候选不应因 SP 修复而标记解决；本次未关闭或评论 GitHub issue #24。既有第 12 页修复继续保留。

同日后续：Original / The Best 的 44 处候选随后全部修复并通过静态与 LRPS2 检查；用户确认提交后，issue #24 已关闭。主版本最终证据见 [`QA_MAIN_LAYOUT_WRITEBACK_20260920.md`](../QA_MAIN_LAYOUT_WRITEBACK_20260920.md)。
