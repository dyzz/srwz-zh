# Q&A 第 12 页排版与 SP 原文差异核查

2026-09-20。第 12 页译文切分已修复；Original 组件及当前 SP ISO 静态回读通过，模拟器验收待做。

## 第 12 页

问题来自 `corpus/zh/menu/nisv-strategy-qa.json` 的文本记录切分：

- `record/004` 末尾的 `0` 与 `record/005` 开头的 `.5%` 被排到两行。
- `record/018` 末尾的 `1.` 与 `record/019` 开头的 `2倍` 被排到两行。
- 同页还有 `格斗武／器`、`伤／害比例`、`3／机` 的切分。

本次调整 9 条记录的边界，逐字拼接的全文完全不变，数字、百分号、拉丁字符和颜色保持不变。
`0.5%`、`1.2倍`、`3机`、`格斗武器`、`伤害比例` 均在同一显示行内；所有文字末字坐标不超过 532。
继续使用原有 19 像素字距和 11 单位行距，空格仍按双字节编码。

Original 使用生产 `build_nisv_strategy_qa` 重建并回读全部 102 页／2609 条记录。
与现有中文组件比较，只改变第 12 页：恢复该页后整个解压块逐字节相同，其余压缩块亦不变。
本次 Original 组件保存在：
`work/analysis/qa-layout-20260920/original/DATA/NISVDATA.BIN`。

完整 Original ISO 构建在输入检查处停止：本地缺少 `rom/original.iso`，也没有当前 Original ISO。
因此没有声称已经生成新的 Original ISO，生产组件缓存也没有被当作完整构建结果覆盖。
语料及 `config/full-story-components.json` 输入锁已更新，恢复原盘后可按常规流程重建。

SP 通过 `tools/special_disc/writeback/build_qa_layout_candidate.py` 原子更新
`build/iso/special-disc/sp-current.iso`，保留原有回执及其他组件信息。
只改 NISVDATA 第 6 个压缩块中的第 12 页；其余 101 页、全部元数据、图片和其他 ISO 区域不变。
压缩槽仍为 39,600 字节，ISO 大小、成员大小和 LBA 不变。
`build_full_text.py` 已接入相同处理，后续全量 SP 构建会从当前语料重新编译该页。
SP 特有页面没有被 Original 页面覆盖。

| 产物 | SHA-256 |
| --- | --- |
| Original NISVDATA 组件 | `5f57fd2117ec98e2f9818a3060c8faa0949f33e765d8a7a5e616a3931912c181` |
| 本次 SP ISO | `e2d95871e0ef66875787d76369d23893f8560cae733cab5105d72f3c5c69133a` |
| 更新前 SP ISO | `d8c91e60993a4fb104632dfb1581a7750c72686389235ffd08bd25c8e6dee21e` |

## SP 是否还有新内容需要翻译

**SP 有改写内容，但本次检查的差异项全部已有中文稿，缺口在写回。**

逐页对照两版日文原始数据与当前 SP ISO：

| 对象 | 数量 | 当前状态 |
| --- | ---: | --- |
| 总页数 | 102 | 没有新增页号 |
| 与 Original 完全相同的原文页面 | 80 | 已复用本篇中文 |
| SP 改动的正文页面 | 22 | 当前仍为 SP 日文原字节；全部有 reviewed 中文稿 |
| 改动的问题标题 | 3 | 同上；主要为队形名加入中点 |
| 改动的关键词摘要 | 12 | 同上；包含术语修正和 SP 删除的内容 |
| 缺少中文稿的差异项 | 0 | 37 条原文与现有译稿的 source hash 全部匹配 |

现成译稿位于 `corpus/zh/special-disc/native-text.json`，ID 前缀为 `sd/nisv/qa/`。
37 条均为 `editorial_status=reviewed`，但也是
`writeback_status=binding_and_layout_pending`、`terminology_check=false`。
“已有审校稿”不能视为“已入盘”或“术语已统一”。例如新增稿中的阵型／资料库菜单名称，
仍需与本篇实际菜单词表核对后，再做颜色分段、页面容量与排版写回。

### 22 页具体差异

下列是原文编辑差异，不据此推断所有游戏机制本身都发生了变化。

| 页 | 原文改动与处理重点 |
| --- | --- |
| 15 | “装甲值降低”一句助词 `が→を`；本篇语义基本可复用 |
| 32 | 删除由机体／驾驶员地形适应推导单位地形适应的整段与表格，只保留地形减伤效果 |
| 35、59 | 补句号；正文语义基本不变 |
| 60 | I力场 EN 消耗 `5→10`，护罩力场 `10→5`；删除光束涂层示例，必须保留 SP 数值 |
| 70、75、88、89 | 操作场所表述改为“整备等”，或“整备／出击前准备等”，不是全新说明 |
| 71 | `ブレイカー→ブレーカー` 表记修正 |
| 79 | `効果のおよぼす→効果を及ぼす` 等日文用字、语法修正 |
| 81 | 自动编成说明由“地形类型”改为“移动类型” |
| 82 | 换乘的日文表记修正 |
| 93 | 重写为 SP 没有 SR 点数，并说明与本篇 NORMAL 难度的关系；不能照搬本篇 SR 获得条件和奖励 |
| 94 | 集市限定为强化零件交易，加入设置菜单入口 |
| 95 | 集市仅出售强化零件，删除购买机体、消耗品套装及行情说明 |
| 96 | 删除行情、拆卸装备、无法买回等扩展说明，仅保留出售强化零件换 BS |
| 97 | 明确 SP 不出售本篇用于支线事件的道具 |
| 99 | `LIBRARY` 改为日文菜单名，并简化一句措辞 |
| 100 | 删除放弃 SR 点数降低难度的建议，改为剧情／挑战模式反复挑战与继承收益 |
| 101 | 中途存档适用剧情／挑战模式；继续游戏入口改为额外关卡主菜单 |
| 102 | 重写通关奖励：资金／PP 奖励、剧情模式机体与武器 15 段改造、挑战全通后特别剧场新增模式 |

12 条关键词摘要涉及：32 页地形效果，51／52 页小队加成，71 页破坏者表记，
93／95／96／97 页删除本篇独有关键词，99 页资料库菜单名，100／101／102 页重玩、继续和通关内容。
三条问题标题对应第 2／3／4 页。

## 验证与重放

- Q&A 专项 **7 项通过**：数字与词组不跨行、行宽、全文与颜色、SP 其他页面／压缩块不变、幂等性、拒绝覆盖非预期译文。
- Original 全块构建及译文逐条回读通过；SP 最终 ISO 成员回读、目录／LBA、目标槽以外 3,791,742,288 字节的哈希比对通过。
- 全套测试运行 399 项，2 项失败、1 项错误、1 项跳过。两项失败为既有剧情“修理屋”和战斗“逃生／脱出”措辞断言，与 Q&A 无关；错误来自 Z:Report 测试缺少 `rom/original.iso`。相关日志保存在本次工作目录。
- 本轮未运行模拟器，运行验收仍为 pending。

```sh
python3 -m unittest discover -s tests -p 'test_*qa*.py'
python3 tools/special_disc/writeback/build_qa_layout_candidate.py
python3 tools/special_disc/verification/audit_qa.py --output manifests/qa-layout-sp-audit-20260920.json
```

机器清单：[`qa-layout-sp-audit-20260920.json`](../../manifests/qa-layout-sp-audit-20260920.json)。
其中保存全部 37 条来源校验、原文差异、译稿状态和第 12 页 ISO 回读文字／坐标。
本次工作目录：`work/analysis/qa-layout-20260920/`。
