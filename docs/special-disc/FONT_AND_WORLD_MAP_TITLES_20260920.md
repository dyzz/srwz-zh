# SP 补字与场间世界地图标题

用户反馈缺字可以补入字库，并要求核对场间地名，包括纽瓦克基地。
结果写入唯一的 `build/iso/special-disc/sp-current.iso`，没有新增常驻候选镜像。

## 字库

全量翻译树审计新增 `‐Ⅰ「倘臻讥踵` 七个字符。既有 3518 条主映射、48 条别名及
36 条源兼容映射全部保持原样；主映射现为 3525 条，普通宽度安全候选槽剩余 26 个。
原有字形的解压像素逐字节不变，仅新分配字槽变化。新字库组件经过 prepare/build/verify、
SP 音频无损搬移检查及 current ISO 回读。SP 语料缺字扫描为零。

“纽”已有字形。地图上的“北阿美利亚大陆　新地球联邦军纽瓦克基地”来自图片，
不会因共享文本字库追加字形而重新绘制。

## 场间地图标题

原盘 201 个 MAPMODEL 成员与本篇 78 个标题模板比对，定位 10 处继承图片；另有
3 处 SP 新增标题（纽瓦克基地、北阿美利亚大陆中部、麦康奈尔基地）。
检查修复前 ISO 发现 4 张继承图片仍是日文，5 张迁移不完整并残留旧笔画，1 张已正确。
原迁移只复制变化字节段，不能可靠清除整张标题中的原有笔画。

`world_map_titles.py` 以冻结本篇中文快照完整覆盖 512×32 标题像素，绑定成员、位置、
原图／旧迁移图／输出图哈希，并拒绝未知前像。10 处均留在原压缩槽内；CLUT、英文
WORLD MAP 副标题、地形文本及解压后其他字节不变。完整构建与增量修补使用同一实现。

13 张最终标题均从 ISO 解压回读、比对冻结像素，并查看拼图确认无日文字形残留。
图片及逐项回执：

- `work/analysis/sp-font-mapnames-20260920/all-map-titles-final.png`
- `work/analysis/sp-font-mapnames-20260920/all-map-titles-readback.json`
- `work/verification/sp-current-world-map-titles/readback.json`

## 验证边界

最终 ISO SHA-256：`d8c91e60993a4fb104632dfb1581a7750c72686389235ffd08bd25c8e6dee21e`。
ISO 大小 3,791,781,888 字节，目录／LBA 不变；10 个声明成员哈希回读通过。
新增回归覆盖旧字形不可改写、字槽不可复用、完整标题替换、未知前像拒绝、原槽压缩
回读及所有非目标字节保持不变。

本次没有把全量审阅文本重新写入 ISO。3 条 HSFC 摘要和旁白 `sd/mtzspros/09/0`
仍超过既有行数上限，详见 `work/analysis/sp-font-mapnames-20260920/preflight-after-font.log`。
这些版面问题单独保留，未删减审阅句子，也不影响此次字库／图片更新的静态证明。
最终 ISO 的 LRPS2 回归通过：第 8100 帧纽瓦克世界地图标题、8580 帧司令室地点字幕、
11100 帧“发令”关卡标题及 15600 帧战术地图均查看确认；源记忆卡哈希不变。
回执在 `work/runtime/lrps2/sp-font-map-titles-20260920/run/receipt.json`，人工查看记录在
同目录上一层的 `visual-review.json`。其余地图标题是静态／ISO 图片证据；各新增字符
没有逐字运行触发，13 张场间地图也未全部经过 PCSX2 人工验收。

12 项针对性回归通过（补字／地图 4、关卡标题 5、字形栅格交接 3）；共享分配审计
`new=0`，图片完整构建组件可生成，地图增量修补再次运行不改变 ISO。
