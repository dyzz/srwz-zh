# GPT6 Pro 校订疑问处理

本轮按用户要求处理校订包的术语／上下文疑问，并单独调查 7 条提取存疑项。以原盘日文、已审定本篇词表和正文为依据，保留 Pro 其他润色结果。

结果：25 条原有术语标记全部结案；另同步 1 条 Q&A 索引。26 条中 22 条调整文字，4 条保留。7 条存疑项全部证实为程序跳转表，不是待翻译文本。此次只修改语料、导出过滤与写回保护，未构建或替换 ISO。

## 术语决定

| 项目 | 处理结果 | 依据／边界 |
| --- | --- | --- |
| メロン・アモス | 保留“阿莫斯甜瓜” | 直接解码 SP 中断对话，吉隆确实以此为食物名；本篇正文 `story/000/system-dialogue/01E420` 同译。 |
| ターンタイプ | `Turn型` | 与本篇 Turn X 图鉴保持大小写一致；本篇相同小队编号对应 GX，不能按编号套译。 |
| 航空ガンランチャー及 MMI-M182 型号 | 保留“航空枪榴弹发射器” | 本篇已审定同名同型号武器 `weapon/0391`、`weapon/0392`；SP 日文仅省去“ガン・ランチャー”中的中点。本轮不另行改武器设定译名。 |
| ジェーン西野 | 保留“简·西野” | 原盘直接核验：SP 为西野，本篇为野口。本篇短名为“简”；保留 SP 原文差异，不主张已判定哪个版本的姓氏设定正确。 |
| 阿莉西娅／塞莉安 | 阿莉西亚／瑟利安 | 本篇已审定地名、人物名；人物长短版各自的信息范围保持。 |
| 包围修正 | 站位修正 | 与本篇 Q&A 一致；Pls、百分比及不计攻击小队的规则保持。 |
| 看破 | 看穿 | 本篇技能表 `skill/intuition`。 |
| 马赫特闪 | 马赫特技 | 本篇特殊能力表及战斗提示图集。 |
| 破坏者／秃鹫 | 破坏者／秃鹰 | 同步 Q&A 071 正文与关键词 070。 |
| 基本编成／均衡编成 | 基本方案／均衡方案 | 采用本篇实际菜单选项，原 COMPDATA `0x7ED30`、`0x7ED50` 直接解码对应两个日文名。本篇旧 Q&A 有“基本指令／基础指令／平衡指令”异文，此处以菜单名为准；本轮未修改本篇。 |
| 阿克艾里昂系列 | 德尔塔／太阳／火星／月亮战机；双翅；DEAVA；GUN 荚舱；末日形态 | 按本篇图鉴和词表统一。保留 Pro 已一致的突击步行形态等表述；SP 的新国连军含义保留，不套用本篇德尔塔图鉴“新帝国军”的原文。 |
| 平行世界武装 | 加纳利·卡弗／杰克切割刃／巴利镰刃 | 按本篇已审定武器表和图鉴统一；保留 SP 的Ⅰ／Ⅱ号机、无珠玉世界和格斗定位。 |
| ファクトリー | 工厂众人 | 修正 Pro 的“人名”判断。格罗玛原文明确为“シャイアの率いるファクトリー”，本篇同段译“夏伊亚率领的工厂”；此处指该商队组织的一行人。 |

所有条目、前后译文、原文哈希和具体依据保存在 [术语决定表](../../config/editorial/special-disc/pro-review-resolutions.json)。

## 7 条误提取的独立调查

这些字节位于 `SLPS_259.20` 的数据表中。原来的字串扫描把一个四字节小端程序地址误读为“汉字＋标点＋零终止”。每项都找到实际使用该表的 MIPS 指令：按索引乘 4 计算表项地址，`lw` 读取目标，再由 `jr` 跳转。不是仅凭文字看起来奇怪就排除。

| 导出原文 | EXE 文件偏移 | 小端地址值 | 读取并跳转的代码偏移 |
| --- | --- | --- | --- |
| 舌( | `0x3B2060` | `0x0028E390` | `0x18EBD4` |
| 芋( | `0x3B2088` | `0x0028F088` | `0x18F468` |
| 声, | `0x3B3590` | `0x002CBA90` | `0x1CC268` |
| 精, | `0x3B35B0` | `0x002CB890` | `0x1CBF84` |
| 姻, | `0x3B3620` | `0x002CF788` | `0x1CFFC4` |
| 斡; | `0x3C0D44` | `0x003BB488` | `0x2BBDC0` |
| 清; | `0x3C0D48` | `0x003BB490` | `0x2BBDC0` |

- 原始附件、历史导出、`submitted_translation`、Pro 的 `review_notes` 和 `review_status` 均保留，便于追溯。
- 当前采用台账新增 `review_resolution`；7 项的 `disposition` 改为 `excluded_non_text`，译文仍为空，没有加入系统或新增原生语料。
- 两个 EXE 导出入口共用的排除表已更新；实际运行当前非关卡导出器确认这 7 项不再进入结果，有效标题仍正常导出。
- 系统文本写回器会拒绝与上述地址字节重叠的文本写入，包括从前方开始、跨入地址的写入。
- 从当前 ISO 回读确认全部 7 处仍与原盘字节一致，无需修复镜像。

复验命令：

```sh
python3 tools/special_disc/verification/audit_exe_non_text.py
python3 -m unittest discover -s tests -p 'test_special_disc*.py'
```

[二进制证据](../../work/authoring/special-disc/review-imports/20260919-gpt6-pro/exe-non-text-audit.json) 包含原始字节、跳转表索引、消费者指令、代码地址及当前 ISO 字节对比。

## 验证与剩余工程工作

21 项 SP 专项测试通过；57 个 Python 源文件、65 个兼容链接和 21 个资源输入自检通过。语料验证保留所有原文身份、提交译文和定位，仅采用记录中的术语变化；数字、控制标记、长短版及Ⅰ／Ⅱ没有随术语替换改变。

再次执行共享字库和既有页面布局预检，问题列表与合并时一致，没有新增问题。此前的 2 个未编码字符、字形覆盖确认、4 处超行、5 个系统槽容量问题，以及 190 个新增字段写回绑定仍属于后续工程工作；术语结案不等于校订稿已经写入 ISO。本轮没有运行模拟器，也没有宣称画面验收通过。
