# SP 当前译稿全量文本候选

2026-09-19：五份既有 SP 译稿共 **8,629 条**已全部接入实际镜像，目标清单没有未写入项。原译稿保持 draft；本次完成的是当前译稿写回，不是全游戏无日文残留或正式发布验收。

## 制品与复现

- 镜像：`build/iso/special-disc/full-text/sp-zh-full-text.iso`
- SHA-256：`57bbe8444972c0c567dfb50c34e843401a9ad6b9082a2a891e9cbeae242a84eb`
- 大小：3,791,781,888 字节。
- 整盘身份、成员哈希及输入清单：相邻 `sp-zh-full-text.json`。
- 各组件、逐条绑定、容量与保护报告：`work/build/special-disc/full-text/`。
- 覆盖清单：该目录下 `coverage.json`。
- 原图片预览及两关候选保留原目录；这份镜像沿用已验证的共享字库及图片候选。

从仓库根目录，使用项目 Python 3.12 及其依赖：

```sh
python3 tools/special_disc/writeback/build_full_text.py
```

该入口顺序构建 system、全量 STAGE、SRVC、frame、图片文字组件，然后应用剧情流程默认展示补丁、装配与逐成员回读。已有组件只装配使用 `--assemble-only`。构建仍依赖保留的两关候选、共享字体候选和既有本篇迁移组件，尚不是只给原盘即可从零构建的发布流水线。

## 覆盖与排版

| 既有语料 | 条目数 | 本次落点 |
| --- | ---: | --- |
| story-dialogue | 5,999 | 24 个剧情 STAGE 块及其分支；包含场景标签 |
| challenge-dialogue | 831 | 18 个挑战 STAGE 块 |
| battle-lines | 123 | 269 处实际字幕引用，原有分行和触发集合保留 |
| frame-text | 286 | 335 个逻辑位置；按记录展开后写入名称、标题、说明、梗概、旁白及 5 处图片文字 |
| system-text | 1,390 | 主程序、COMPDATA、滚动字幕及模板列表；全部写入 |

STAGE 连同本篇复用文本、条件和名称共有 **10,113 条绑定**，其中 39 个编队名称逻辑位置展开为 **280 个原生字段**。42 个块全部压缩回原槽，最小余量为 135 字节（chunk 051）；HB 边界、解压大小、未知字节与事件参数保持不变。

- 对白沿用本篇共享 profile：首行 21 格、续行 20 格并保留缩进、最多 3 行。场景字幕与条件单独处理，不套用对白缩进。
- VT1 的 24 页按每行 28 格、56 字节加 LF 写入；第 40 块保留标题／说明／末行确认问题，第 50 块保留 7 行任务简报。可见空格用双字节 `0x8140`。
- HSFC 摘要最多 3 行、每行 21 格；不足 3 行补空槽。STAGE 流程梗概按 29 格折行，保留段首缩进及段落边界。
- 10 段旁白保持原窗口的行数上限与可见行宽，保留空行。
- `Orange` 写入已验证的原生 23 字节名称字段；旧导出报告的 9 字节只是已使用串长。`Keep On Movin'` 的完整英文通过 21 个章节标题的已拥有字符串池重排容纳，相关原生标题指针一起更新。两者均未缩写或改名。
- 结算“序章／进行中”和 3 张新世界地图标题已写入。冻结像素在 `config/assets/special-disc/full-text-image-labels.json`；正常构建只读快照。CLUT、英文副标题、非目标像素、容器头和尾数据保持原样。

## 绑定与字库

使用本篇同一码表和字库，沿用两关候选新增的 42 个映射，不建立 SP 专用分配表。原主程序的两处特殊范围补丁保持 `9f820134`。全量 STAGE／frame 输出中未分配的非 ASCII 可见字符为 0，装入的解压字库哈希与共享验证清单一致。

歧义按原生位置固定，不使用导出工具历史上的“取第一种”作为写回规则：

- 19 处首批原生补充仍在 `stage-native-overrides.json`。
- `stage-context-reuse.json` 有 871 个补充位置：105 个相同说话人及相邻原文的唯一答案、26 个相邻场景标签、其余为明确编辑选择。结局重复段统一取本篇 stage-145 分支中原文对应的唯一译稿；性别相关 `$n` 称呼按剧情位置分别绑定。
- 原五份译稿内容与状态均未改写；新绑定也保持 draft。
- 开发块 stg_500／501 不在玩家文本范围，本次没有将其缺少译文的记录强行标为完成。

串内引用检查曾发现两类数值碰撞。chunk 056 是已确认的原生机体名称字节，不是指针；chunk 021 的 `0x0080F000` 是 map-script `0x13AB` 的双 16 位参数。已检查 SP 可执行文件分发表和处理函数：字段被拆成低 16 位／高 16 位整数，特殊值 `0xF000` 转为 -1，没有当作文本地址解引用。消费者指令和完整源记录锁在 `config/products/special-disc/stage-scalar-contracts.json`。其他未知指针候选继续拒绝写回。

## 验证边界

10 个成员有变化；ISO 的成员路径、大小与 LBA 均保持一致。所有声明成员都从最终 ISO 回读比对；声明成员之外 **3,533,054,509 字节**逐字节不变。字库、图片预览及各文本组件的重叠由前像检查和明确块范围合成，未用最后一份完整成员覆盖前一步成果。

SRVC 的 25,526 个唯一文本、59,262 条记录均已有译文并通过回读；新增 SP 123 条全部覆盖。字幕索引、触发元数据及未索引尾部按既有写回器保护。

独立校验器 `tools/special_disc/verification/verify_full_text.py` 已直接从最终 ISO 重新解析并核对 8,754 条对白／条件记录及其说话人前缀、280 个编队字段、147 个 frame 目标、1,679 处系统写入，以及 12 种滚动字幕的 35 处引用。结果在 `independent-readback.json`；综合记录在 `validation.json`。

专项测试 14 项与既有重排安全测试 9 项均通过。全套测试 348 项：344 通过、1 跳过、2 失败、1 错误；三项问题与本次前的结果相同：本篇“修理屋”预期、本篇 battle:00113 的“逃生／快脱出”预期，以及缺少 `rom/original.iso` 的 Z:Report 测试。没有改动这些无关语料和测试预期。日志保存在组件目录的 `focused-tests.log` 与 `tests.log`。

剧情流程补丁前的译稿镜像（SHA-256 `854028dcbe6990f523eefb119f5b384fbaefff6be7b37de7fc924f0d709cd828`）已由 LRPS2 完成剧情和挑战入场抽查：24 页中的剧情组说明与 NORMAL MISSION 1 简报样本均为中文，标题、正文和确认行未见截断；序章旁白保留段落空行，并修正了“下达”跨行；两关场景字幕、姓名和多句对白正常。该译稿版本的证据在 `work/runtime/lrps2/sp-full-text-20260919/{story-first-line-final,challenge-entry-final}/`，`visual-review.json` 逐图记录人工视觉判断和哈希。运行器的 passed 本身只代表执行及画面尺寸检查，视觉结论来自实际截图审看。PCSX2、全路线战斗／存读档与译文审校仍未完成。

图鉴差异、Q&A 新内容以及此前未形成这五份译稿的图片／文字仍属于后续补译范围；本报告的 100% 分母仅为当前 8,629 条译稿。

## 剧情流程默认全部展示（2026-09-19）

当前镜像已接入 `tools/special_disc/writeback/chart_visibility.py`。在 `STAGE.BIN` 第 0 块的流程图初始化中，将偏移 `0xD88`（VA `0x805378`）的 `sltu t2, zero, v0` 改为 `addiu t2, zero, 1`，让 Special Disc 的 21 个有效节点不再按存档进度隐藏。Z 的 110 个表内节点使用原生负 ID 分支，保持默认可查看；灰色节点是其原生外观，选中后可以显示标题和打开梗概。

补丁只作用于流程图内部的显示结果。表结束标记 `0x46`、负 ID 分支、共用进度查询 `0x155040`、可玩关卡进度及存档写入函数均未修改。完整初始化函数前像 SHA 和局部指令逐字节锁定，遇到漂移即拒绝。默认展示版本（`521931fb…`，按键说明修正前）与此前全量文本镜像相比，仅 `DATA/STAGE.BIN` 有变化；其解压第 0 块只变这一条指令，其余文本、图像、码表和主程序完全相同。重压 41,491 字节，原槽 44,016 字节，后续块与 LBA 不动。

默认展示版本已通过 17 项 SP 专项测试和完整译稿独立 ISO 回读，额外逐字节确认流程块仅有目标指令变化。静态节点表、前后成员身份和边界证据在 `work/analysis/sp-chart-default-20260919/static-evidence.json`。

LRPS2 使用同一份无 SP 关卡进度的隔离存储卡，已确认：SP 21 个节点全部显示，能打开“想吃所以合体”；切换 Z 流程后能打开第 2 话“启程之日”，向下滚动后能打开第 41 话“交叉点”。已审看截图，标题与中文梗概未见溢出或截断。源存储卡和隔离卡运行前后 SHA 均为 `57c9cd6baee5499a3b374256351add3ad483b46717fa16b899680229c492fd21`。默认展示版本的运行与视觉证据在 `work/runtime/lrps2/sp-chart-default-20260919/z-detail/` 及相邻 `visual-review.json`。这是流程图代表节点的 LRPS2 验证，尚不是每份梗概、完整游戏流程或 PCSX2 的人工验收。

## 两套剧情流程的按钮说明（2026-09-19）

两页共用的三条按钮文字及各自的切换提示已全部写回：`：确认`、`：返回`、`：加速`、`：切换至Special Disc`、`：切换至SRW Z`。这五条属于原先未翻译的资料库补漏项目，新增配置为 `config/editorial/special-disc/chart-key-help.json`，独立于既有 8,629 条译稿计数。`write_frame_text.py` 会校验原文、原生容量和全部 8 处指针引用，并按共享码表重新编码；没有修改字库或按钮图标。

原日文“決定”的码位在中文字库中会显示成“糕定”。本次将对应文字重新编码为“确认”，并处理同一区域的“返回”“加速”和两条切换提示。画面迭代中发现“切换至Special Disc流程”在右端截断，最终保留完整 `Special Disc` 名称，缩短为“切换至Special Disc”。可见空格仍为双字节 `0x8140`。

与默认展示版本相比，解压内容仅五个 NUL 结尾文字槽变化，8 处指针和其他字节不变；还原这五个槽即可得到旧版本的解压 SHA。仅 STAGE 第 0 块重压，后续 67 块、全部其他成员、主程序、字库及默认全部展示补丁不变。独立 ISO 回读新增逐条核对这 5 条说明，17 项 SP 专项测试通过；静态范围证据见 `work/analysis/sp-chart-help-20260919/static-evidence.json`。

按钮说明版本（`0e5e7396…`）已由 LRPS2 验证 SP 流程、SP 详情、Z 流程、Z 详情四个画面：按钮文字清晰完整，切换提示无截断，图标与文字未重叠。源卡和隔离卡运行前后内容一致。证据在 `work/runtime/lrps2/sp-chart-help-20260919/final/` 与相邻 `visual-review.json`；PCSX2 人工验收仍待完成。

## ARMSX2 与本地镜像整理（2026-09-19）

ARMSX2 的 SP 扫描目录已从旧 `build/iso/sp-image-preview/` 切换到 `build/iso/special-disc/full-text/`。启动后日志及重建的游戏列表缓存已确认读入当前 `sp-zh-full-text.iso`，旧图片预览已不在游戏列表中；无需另复制一份 ISO。

已核对 SHA 后删除 `work/build/special-disc/preview/sp-image-preview.iso` 的重复副本，保留构建所需的 `build/iso/special-disc/preview/sp-image-preview.iso` 与 `text-candidate/sp-text-canary.iso`。9 月 12 日旧资料库测试 ISO 已移至用户废纸篓，可恢复；原盘、本篇 CHD、存储卡与即时存档均未修改。配置备份和整理清单见 `work/maintenance/sp-iso-cleanup-20260919-204924/report.json`。本次为游戏列表安装与整理，不新增 ARMSX2 游戏运行验收结论。

## 话数标签补齐（2026-09-19）

用户在 ARMSX2 截图中指出“最终话”仍有错字。原因是流程记录中的 16 字节话数标签仍为日文编码：`最終話` 的“終”在共享中文字库中显示为其他汉字。现已按共享码表重新编码 SP 的 2 处、Z 的 3 处为 **最终话**；另外 123 个普通话数标签统一为 **第N话**，数字保留。3 个原生空标签仍为空，标题、梗概与节点二进制字段不变。

表结构与编辑规则在 `config/editorial/special-disc/chart-episode-labels.json`，由 frame 写回器检查原文、16 字节槽、填充、表指针和终止记录后写入。独立回读确认 128 个非空标签（含 5 个最终话）、3 个空标签和此前 5 条按钮说明全部正确；17 项 SP 专项测试通过。还原话数标签后可得到上一镜像的完整解压流程块 SHA，其余成员及后续 STAGE 块一致，证据在 `work/analysis/sp-chart-episode-labels-20260919/static-evidence.json`。

话数标签版本（`7a96821f…`）的 LRPS2 实际截图已确认普通话数标签和 SP“跨越黑暗历史”的最终话详情字形正常、无截断。运行证据在 `work/runtime/lrps2/sp-chart-episode-labels-20260919/run/` 与相邻 `visual-review.json`。ARMSX2 已登记的 ISO 路径不变；已运行的旧会话需重新启动游戏读取新镜像，本次未中断用户正在运行的 ARMSX2。

## 后续校订稿状态（2026-09-19）

GPT6 Pro 用户校订包已合入作者语料，但尚未覆盖本页所列 ISO。本页 ISO／回读证据仍属于校订前内容。新增 190 个原生字段的绑定、字库和排版预检结果见 [Pro 校订合并记录](PRO_REVIEW_IMPORT.md)。

## Z 三个最终话标题补齐（2026-09-19）

旧 `migrate_flow.py` 把 Z 表记录数写成 107，实际有 110 条，因此最后三个最终话的独立标题字段没有写入中文。这三条是本篇既有结局分支，并非 SP 新增关卡。原版 v0.4.2 CHD 的 COMPDATA 已直接解压核对：三个标题各有 3 处话名引用，9 处均为正确中文；原版没有同样的漏写。证据在 `work/analysis/sp-z-ending-titles-20260919/main-game-readback.json`，属于盘内文本回读，并非本轮原版模拟器验收。

SP 现引用本篇 `corpus/zh/menu/stage-names.json`，分别写入“我的未来，大家的未来”“走向无尽战斗的轮回”“我的未来，你的未来”。绑定配置为 `config/editorial/special-disc/chart-z-ending-titles.json`。三个 64 字节标题槽检查原文哈希、填充、容量与共享码表回读；构建锁定绑定配置及原版话名语料。历史迁移器也改为覆盖 110 条，并检查表指针、终止记录和无遗漏。

独立验证器回读完整 110 个 Z 话名：前 107 条与固定中文基线逐字节一致，末尾 3 条与本篇语料一致，均无残留假名。只有三个标题槽的解压字节改变，其他成员、后续 STAGE 块、话数标签、按钮文字及字库均保持原样。17 项 SP 专项测试通过，静态范围证据在 `work/analysis/sp-z-ending-titles-20260919/static-evidence.json`。

当前镜像的 LRPS2 已实际打开三个最终话详情并审看截图：标题和“最终话”字形正确，未见截断。证据在 `work/runtime/lrps2/sp-z-ending-titles-20260919/visual-review.json`。使用隔离存储卡，源卡及副本运行前后均未改变。ARMSX2 登记路径保持不变，重新启动游戏即可读取此次更新；本轮未中断用户的 ARMSX2 会话。
