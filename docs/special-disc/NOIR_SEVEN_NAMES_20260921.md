# 诺瓦尔7七人定名

2026-09-21，用户明确确认以下中文名，作为SP对应人物的正式译名。

| 日文身份 | 原译 | 正式译名 |
|---|---|---|
| オーネ | 奥内 | 伊内 |
| ツバイ | 茨瓦伊 | 贰威 |
| トロー | 特洛 | 叁洛 |
| シジョウ | 四条 | 四条 |
| サンク | 桑克 | 伍克 |
| ヘクス | 赫克斯 | 陆克斯 |
| セプテ | 塞普特 | 柒普特 |

## 适用范围

正文按日文实体和稳定ID修改，不按中文子串全局替换，也不把トロー误命中アウトロー等较长词。四条保持原字面，只将词表状态提升为用户已确认。

已同步12条剧情正文、6条说话人名、12个驾驶员名称字段，以及18条审阅汇总中的当前采用译文。旧回稿的 `submitted_translation`、日文、源文哈希、定位和历史送审ZIP保留原样。词表和人物参考卡中的7人均标记为已确认。

第06～08话此前已正式采用，这次是其后的命名决定；回收旧包时不得用旧人名覆盖此决定。后续新送审包从当前语料和词表生成。叁洛／アウトロー的姓名尾音并未因此在中文自动恢复，不能宣称该近音笑点已解决。

## 字库

“贰、叁、柒”需要追加字形。全局共享字库审计同时发现现有本篇正文 `story/015/dialogue/01.01/0017` 所需的“辗”，因此按既有追加分配流程补齐4字；未修改该本篇台词。

4个新槽为4360（叁）、4361（柒）、4364（贰）、4367（辗）。旧字符编码和槽位不变，剩余普通宽度候选槽22个。prepare/build/verify和SP字体组件安装已完成，7个正式姓名均通过编码解码往返。

重新生成字体时，既有构建器将 `.`、`．` 两槽2836、4467按项目已实施的 `copy_original_japanese_period` 策略恢复为原生8144字形。与当前ISO实际解压字体比较，变化严格限于4个新增槽和这2个点号槽；点号与原生8144像素逐字节相同，其他旧字形不变。此处明确记录额外差异，不将其称作纯粹只补3字。

本次证据、前像和组件保存在 `work/authoring/special-disc/noir-seven-names-20260921/`，其中 `changes.json` 是逐条改名记录，`font-verification.json` 是字库范围验证。

后续完整构建使用已刷新的SP字体输入锁。ISO和审阅站发布结果见本页后续回执；未进行模拟器运行验收。

组件验证：STAGE 011 的12条正文和7个说话人名均已试写回读，压缩为15,562/16,688字节；系统组件构建及90项SP专项测试通过。此前驾驶员字段只有语料更新，现补充 `pilot-names.json` 固定位置契约和生产写回，包含四条在内共7人、14个显示名/名字字段。每次构建验证原始字段字节、位置、容量、语料来源及审阅状态；只允许原始日文前像或同版本已写入值。独立ISO验证器必须从最终COMPDATA解码并核对全部14字段。全部SP输入锁核验通过。

完整构建时进一步发现：旧合成流程只核对字体哈希，未把新字体流放入固定基线。现将经验证的627 KB级压缩字体作为输入锁定，并在最终合成时替换现有626,896字节字库槽；新流626,851字节，余45字节清零。音频和其余VT1字节不变、EXE偏移表不变；超出原槽时明确失败，不能自动挪动音频。新增3项测试覆盖范围保护、身份不符和超容拒绝。

## 提交和审阅站发布

- 定名、正文、说话人和驾驶员字段写回提交：`091a3b8a3434bfd052ba3191372a1a2b8b756757`。
- 字库最终合成修复：`0b9b385`；将锁定的压缩字库装入既有槽位，不改变音频和归档偏移。
- 最终全仓502项测试通过；站点数据检查、55项审阅测试、8项部署测试及生产构建通过。
- 审阅站已部署 `091a3b8-noir-seven-20260921`，使用定名提交对应的语料。12条改名正文、110次相关说话人显示、7个人物条目及14个名称字段全部匹配。
- 源站8个目标JSON的HTTP响应与生产产物哈希一致；发布前后2611条用户建议逐条相同。数据库部署前在线备份及完整性检查通过。
- 2026-09-21 16:15（UTC+8）公网HTTP核对：SP人物页、搜索索引及007/008/009/011四个剧情数据均为新版，站点索引和SP索引仍命中旧CDN缓存。
- 未打开浏览器，未调用CDN连接器或操作控制台。手动刷新清单包含两个索引及本次修改的STAGE 011，共3条；静态资源预热清单10条。副本为 `/Users/nate/Downloads/noir-seven-cdn-refresh-urls.txt` 与 `/Users/nate/Downloads/noir-seven-cdn-prefetch-urls.txt`。

完整构建和发布证据位于 `work/verification/noir-seven-build-publish-20260921/`。CDN状态为 `manual_pending`，未进行LRPS2或PCSX2运行验收。

## 最终ISO回执

- 统一入口 `python3 tools/build_editions.py --editions sp` 完成，构建源码为 `0b9b38534710460f8b063c569c9bf97eca7c8aff`。
- 输入摘要：`71320694fc7939523b7e8588acaaa27b232054c6bb604b13013a6ad19a94b395`。
- 镜像：`build/iso/special-disc/sp-current.iso`，3791781888字节；SHA-256：`863e715f0035d9726f85404adab911813defe14b8a4d9c89d093dd982c11850d`。
- 全量静态组件、固定成员大小/LBA、独立文本回读和批次回执验证通过。
- 从最终根目录ISO直接回读7人14个驾驶员字段；独立STAGE报告核对7个说话人名及第06～08话1015条当前绑定；66处小队名均为“013特别小队”。
- 最终ISO字库与改名前ISO解压字库比较，差异恰为2836、4360、4361、4364、4367、4467六槽，新增字与既有原生点号策略均匹配；其余字形不变。
- 未构建本篇Original/Best镜像；原有未提交文档保持原样。本轮仅本地commit，未push。运行验收保持 `not_tested`。
