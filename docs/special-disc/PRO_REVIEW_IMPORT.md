# GPT6 Pro 校订包合并记录

用户要求整体尊重校订结果。本轮完成文字合并、原文身份校验、独立组件与排版预检；尚未构建新的 ISO。

后续更新：用户授权处理疑问后，25 条术语／上下文标记已结案，另同步 1 条 Q&A 关键词；7 条存疑项已独立确认是 MIPS 跳转表误提取并排除。具体采用差异和二进制依据见 [疑问处理记录](PRO_REVIEW_RESOLUTION.md)。下文数量及“保留标记”等描述是首次合并时的历史快照。

## 采用结果

| 项目 | 数量 |
|---|---:|
| 校订包记录 | 2,282 |
| 非空校订译文 | 2,275 |
| 包中实际文字修改 | 527 |
| 审阅后保留 | 1,748 |
| 疑似误提取、留空保留 | 7 |
| 用户先前要求优先的例外 | 2 |
| 更新审校信息的现有语料条目（去重） | 1,677 |
| 现有语料文字改动（去重） | 282 |
| 新收录的原生文本字段 | 190 |

527 是导出记录层面的修改数；同一话名／梗概在多个原生落点和语料副本中重复出现，合并时归入同一语料条目。空白校订不会覆盖原文。

两条例外是切换按钮：校订包加回“流程图”，与用户此前去掉“流程”的要求及已有宽度验证相冲突，继续采用 `：切换至Special Disc` 与 `：切换至SRW Z`。除此之外，接受的文字未另做整体润色或压缩。25 条术语／上下文核对标记保留，未把候选术语当作已完成全项目统一。

## 落点

- [完整采用台账](../../corpus/zh/special-disc/reviewed-non-stage-text.json)：提交译文、采用译文、备注、原文哈希、来源与处理去向。
- 原有 `system-text.json`、`frame-text.json`、`battle-lines.json` 已合入校订并标明审校来源。
- [新增原生文本语料](../../corpus/zh/special-disc/native-text.json)：190 个此前写回器未覆盖或尚未绑定的字段，保留定位，状态为 `binding_and_layout_pending`。
- [合并报告](../../work/authoring/special-disc/review-imports/20260919-gpt6-pro/import-report.json)
- [合并完整性验证](../../work/authoring/special-disc/review-imports/20260919-gpt6-pro/merge-verification.json)
- [工程预检](../../work/authoring/special-disc/review-imports/20260919-gpt6-pro/engineering-preflight.json)

原压缩包、原样解包文件及合并前的三份语料快照保存在 `work/authoring/special-disc/review-imports/20260919-gpt6-pro/`。附件作为编辑数据读取，没有执行其代码或把包内说明当作额外用户命令。

## 写回预检与当前镜像

当前 ARMSX2 使用的 ISO 仍为校订前版本 `57bbe8444972c0c567dfb50c34e843401a9ad6b9082a2a891e9cbeae242a84eb`，本轮未覆盖。现有 ISO 清单／组件回读证据仍对应旧语料，不能证明当前校订稿已写入。

- `倘`、`讥` 尚无当前编码；`‐`、`Ⅰ`、`「`、`臻`、`踵` 虽可编码，但不在已验证共享字库覆盖清单内，需要字形验证或正式补字。
- 3 处节点简介超过 21 格 × 3 行限制；1 处结尾旁白由 13 行增至 14 行。完整稿保留，未擅自删改。
- 3 处主程序文本、2 处 COMPDATA 文本超出既有槽容量。
- 190 个新增字段尚需接入写回，其中资料库 116 条（图鉴 79、Q&A 37）需要字段／页面排版，不能按原长度直接覆盖。
- 战斗台词独立组件成功，59,262 个原生记录全部回读，SP 台词转义换行与 20 格内部行宽约束通过。尚未装入 ISO。
- 17 项 SP 专项测试通过；55 个 Python 源文件、65 个别名及 21 个资源输入自检通过。

具体问题：`sd/hsfc/02`、`sd/hsfc/17`、`sd/hsfc/27` 行数超限；`sd/flow/synopsis/10` 含未编码的“讥”；`sd/mtzspros/09/0` 超一行。系统容量问题为 `sd/exe/3C4820`、`sd/exe/3C49C0`、`sd/exe/3C4B40`、`sd/compdata/9CDA0`、`sd/compdata/7B680`。

## 后续写回顺序

1. 为 190 个新增字段建立原文、指针、空间校验的写回绑定，纳入全量构建覆盖统计。当前五份既有语料的统计不涵盖新增语料，不能单凭旧统计宣称完整写回。
2. 按共享字库契约补齐或验证上述 7 个字符，保持既有码表分配稳定。
3. 调整简介、旁白和定长槽的布局或容量；如必须改写措辞，单独记录差异，尽量保留 Pro 表述。
4. 重建相关组件和 ISO，完成原生回读与实际页面验证后，更新 ARMSX2 所用镜像。

导入工具为 `tools/special_disc/translation/import_pro_review.py`，默认生成计划，`--apply` 才更新语料；已有台账会拒绝重复覆盖。预检工具为 `tools/special_disc/verification/preflight_pro_review.py`。
