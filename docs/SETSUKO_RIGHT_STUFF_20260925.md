# 节子路线标题：可造之材

2026-09-25，按用户明确指定，将节子开场介绍副标题和第 1 话标题从“太空先锋”统一为“可造之材”。开场保留原有装饰：`〜可造之材〜`。菜单、存档及流程图使用同一标题；人物介绍正文和剧情对白不作扩展修改。

同步现有生产语料的五处入口，以及第一关入场标题的冻结索引图片。只对第 0 张标题进行显式重制，使用当前发布 ISO 中已有的“可造之材”字形和既定排版；其余 106 张标题的冻结数据保持逐项一致。原始调色板、非图像字节及压缩槽边界保持不变，编码后 1011 字节，原槽容量 1776 字节。

标题索引预览已检查。普通构建仍只注入冻结图片，不重新渲染。构建使用统一 Original / Best 流程及内置内容回读，不额外重复全量测试；本次不包含 PCSX2 人工运行验收。
