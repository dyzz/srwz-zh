# 搜索页补给与干扰说明漏译

反馈人：**蒙古王者风行烈**。2026-09-09 由维护者明确补充署名。两张玩家截图分别显示搜索界面 `EN补给` 和 `干扰功能` 的底部效果说明仍为日文；反馈截图未附精确 ISO 身份，不强行推定截图来自哪一个发行版本。

两项合并为同一搜索界面问题登记，记录编号 `SEARCH-SQUAD-HELP-20260909`。GitHub：[issue #23](https://github.com/dyzz/srwz-zh/issues/23)。

## 来源与修复

列表中的能力名称已有中文，但其效果说明来自两个独立的 `DATA/COMPDATA.BN` 固定文本槽，未进入此前的中文覆盖。

| 项目 | 解码偏移 | 实际写入的两行译文 |
| --- | --- | --- |
| EN 补给 | `0x73E50` | 小队内每有1台持有补给装置的单位，<br>小队全机便获得EN回复10%的效果。 |
| 干扰功能 | `0x73EC0` | 小队内有持有干扰功能的单位时，<br>最终命中率和回避率均提升10%。 |

修复提交：`bc0c667`；语料为 `corpus/zh/menu/remaining-ui.json`。原版和 Best 使用同源文本，本轮修复构建的两版均已更新。写入保持固定槽边界、指针和控制数据，不改变补给或干扰的游戏计算逻辑。

补给效果另与两版原盘的详细帮助 `0x77AF0` 交叉核对：我方阶段开始时，按“持有补给装置的机体数 ×10%”回复小队各机自身的最大 EN。由原版明确公式可知，3 台对应 30%。这项结论来自原版文本，不冒充本次运行中测量过 EN 数值。

## 静态及成品验证

- 两条文本均从本轮 Original / Best 成品 ISO 独立读取并解压；译文、NUL 终止及剩余零填充匹配。
- 原始文本跨度分别为 110 和 92 字节，译文含结束符分别使用 72 和 64 字节，未扩展槽位。
- 完整 259 项测试通过，生产组件构建、Original 全量语义回读、Best 原生构建及回读通过。
- 同批双版构建的输入摘要、回读凭据和实际 ISO 哈希已通过 `verify_editions.py` 核对。

| 版本 | 验证 ISO SHA-256 |
| --- | --- |
| Original | `c0e4e751d7705390766ec69fc921496fe295c7a8afb53c7f64efa6ddd5512b8a` |
| Best | `27e097d94403ee0e765741ac8821730dc095f5863ff4dcb382247a67bdd754e2` |

本轮验证绑定上表的精确哈希。共享的 `current` 路径可被后续任务更新；Best 最终复跑使用本批隔离构建保留的同哈希镜像，避免并行构建替换成另一版本。

## 运行验证

运行检查使用已锁定的 LRPS2 x86_64 核心、Software renderer 和独立记忆卡副本。目标路径为：读取第 22 话场间存档 → 机体 → 搜索 → 小队奖励 → EN补给／干扰功能。检查两行中文显示、相邻修理说明、上下切换及返回搜索分类／机体菜单。

**Original 与 Best 均通过。** 每版冷启动至第 4300 帧，分别检查了补给说明、干扰说明、相邻修理说明、返回搜索分类、返回机体菜单这 5 个稳定区域。全部采用 0 容差 dHash 断言，并人工逐图确认目标中文、两行布局及菜单返回。

Original 从已查看的探查画面锁定参考后重新运行；Best 的两条目标说明和修理说明沿用相同的参考哈希。Best 返回分类页的高亮区域与 Original 相差 1 个 dHash 位，经图像核对后为 Best 单独锁定参考并重新运行，未放宽断言容差。

两版目标文字区域哈希分别相同：补给 `0130f0c0c8480000`，干扰 `016060e0a0200000`。两个正式 session 的源记忆卡前后 SHA-256 均为 `78256c215676e5f732e7b5c1373ad718a1e56fe411334c4b5491a174ffdc3ac8`，隔离副本检查通过。

- [机器可读验证摘要](../manifests/ui-search-squad-help-20260909.json)：包含反馈署名、截图哈希、两版 ISO 身份、逐帧动作、区域断言和 receipt 哈希。
- 原始运行凭据及截图：`work/runtime/lrps2/search-squad-help-20260909/original-locked/`、`best-locked/`，保留在本地忽略目录。
- 单独静态回读：`work/verification/ui-omissions-fix-20260909/reported-two-final-readback.json`。
- 441 个已提交源码／语料／配置文件与已测试构建输入快照逐个 SHA-256 匹配，无差异。

本 issue 的两条说明已完成 LRPS2 运行验证。PCSX2 未执行；不把本记录扩大为新增名称表或充能贴图的运行验收。
