# 流程与隐藏要素攻略

`srwz-z-flow-guide.html` 是面向玩家的离线单文件攻略。页面已内嵌样式、脚本、流程
与隐藏要素，不依赖网络资源；直接用浏览器打开即可。

`data/` 保存攻略正文使用的隐藏要素、流程补充和资料表源数据。攻略生成器及其内部
验证记录不属于 v0.3.0 游戏补丁构建闭包，需要追溯时使用 Git 历史。章节和路线的
技术映射保留在 `../docs/STAGE_ROUTE_MAP.md`。
