# 字库

`source/` 只接受具有明确许可证并记录版本与 SHA-256 的字体源文件。

`generated/` 保存 rasterize、编码映射和压缩过程中生成的文件，不进入 Git。可发布的字符映射、生成参数和哈希清单应写入 `manifests/`。

字库生成必须满足：

- 相同输入产生相同输出；
- 编码槽位发布后保持稳定；
- 未映射字符使构建失败；
- 记录字号、基线、描边、抗锯齿和像素格式；
- 对所有游戏字体样式进行运行验证。
